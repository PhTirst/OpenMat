[package]
license = "AGPL-3.0-only"
name = "openmat-sparse-provider"
version = "0.1.0"
edition = "2024"
rust-version = "1.90"

[dependencies]
faer = { version = "=0.24.4", default-features = false, features = ["std", "sparse-linalg"] }
openmat-array = { path = "../openmat-array" }
openmat-linalg = { path = "../openmat-linalg" }

[lints]
workspace = true
