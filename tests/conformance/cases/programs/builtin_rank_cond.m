threshold_low = diag([1, 4.440892098500626e-16]);
threshold_high = diag([1, 1.1102230246251565e-15]);
a = [1, 2; 3, 5];
singular = diag([1, 0]);
empty = zeros(0, 3);
openmat_result = [ ...
    rank(threshold_low), rank(threshold_high), ...
    cond(a), cond(a, 1), cond(a, 'inf'), cond(a, 'fro'), ...
    cond(singular), cond(empty) ...
];
